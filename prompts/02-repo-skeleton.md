1. Add a `.gitignore` for Node, Playwright (`node_modules/`, `test-results/`,
   `playwright-report/`, `blob-report/`, `.env`), and OS junk files.
2. Create the directory skeleton with a `.gitkeep` in each empty folder:

   docs/
   docs/discovery/
   prompts/
   tests/web/
   tests/api/
   src/web/pages/
   src/web/fixtures/
   src/api/clients/
   src/api/schemas/
   src/data/
   .github/workflows/

5. Write a placeholder README.md with just the project title, the two test targets, and a
   one-line statement that the repo was created from scratch for this exercise.
6. Commit and push per the git workflow in CLAUDE.md.

Then show me the repo URL.
